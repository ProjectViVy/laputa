---
stepsCompleted:
  - step-01-document-discovery
  - step-02-prd-analysis
  - step-03-epic-coverage-validation
  - step-04-ux-alignment
  - step-05-epic-quality-review
  - step-06-final-assessment
documentsInventory:
  prd: "_bmad-output/planning-artifacts/prd.md"
  architecture: "_bmad-output/planning-artifacts/architecture.md"
  epics: null
  ux: null
date: "2026-04-13"
project: "天空之城 (Laputa)"
---

# 实施就绪性评估报告

**日期：** 2026-04-13
**项目：** 天空之城 (Laputa)

---

## 文档清单

| 文档类型 | 文件 | 状态 |
|---------|------|------|
| PRD | `_bmad-output/planning-artifacts/prd.md`（16.2KB） | ✅ 已发现 |
| 架构文档 | `_bmad-output/planning-artifacts/architecture.md`（52.8KB） | ✅ 已发现 |
| Epics & Stories | 未找到 | ⚠️ 缺失 |
| UX 设计 | 未找到 | ⚠️ 缺失 |

---

## PRD 分析

### 功能需求（FR）

FR-1：主体初始化 —— 用户可以创建一个新的天空之城记忆库，并生成最小可运行的身份、状态与记忆布局。

FR-2：日记写入 —— 用户可以写入新的日记条目，并指定或推断时间、分区、标签、来源与情绪锚定信息。

FR-3：记忆筛选 —— 系统可以对新增内容执行可解释的保留判断，包括存储、丢弃、合并或加热既有条目。

FR-4：统一记忆记录 —— 系统可以将事件、关系、状态、摘要胶囊与主体信号保存为统一结构的记忆记录，并支持稳定 ID。

FR-5：节律整理 —— 系统可以在节律点对新增素材进行整理，并生成至少周级摘要胶囊。

FR-6：唤醒包生成 —— 系统可以输出一个用于 AI 启动时注入的唤醒包，默认覆盖身份、近期状态、最近周期摘要、关键关系和关键任务。

FR-7：时间流召回 —— 用户或上层 agent 可以按时间窗口、周期层级或最近阶段召回相关记忆。

FR-8：深度检索 —— 用户或上层 agent 可以对记忆库发起按需深检索，以获取与当前问题最相关的历史条目。

FR-9：关系记忆 —— 系统可以记录并召回人与人、人与项目、人与主体之间的最小关系变化与重要互动，MVP 至少支持关键关系字段与近期关系摘要。

FR-10：情绪锚定 —— 系统可以记录记忆条目的情绪权重，并使其影响保留、强化或召回顺序。

FR-11：手动治理 —— 用户可以对记忆条目执行至少以下干预：标记重要、标记遗忘、设置情绪锚点、手动回看。

FR-12：归档候选 —— 系统可以识别低热度、久未调用内容，并将其标记为归档候选。

FR-13：归档导出 —— 系统可以将归档候选打包导出，并保留用户后续解包与考古的能力。

FR-14：数据导出与迁移 —— 用户可以导出其主体定义、摘要胶囊与核心记忆记录，用于迁移到新设备、新模型或新宿主。

FR-15：工具化接口 —— 系统可以通过 CLI 与 MCP 暴露初始化、写入、整理、唤醒、召回、检索、标记与归档等能力。

**FR 总计：15 项**

---

### 非功能需求（NFR）

NFR-1：纯 Rust 运行 —— 系统应在无 Python 运行时依赖的前提下完成核心安装、初始化、写入、整理、召回、检索与导出流程。

NFR-2：离线可用性 —— 系统应在无外部网络条件下完成全部 MVP 核心能力，且本地数据读写与召回能力不依赖远程服务。

NFR-3：唤醒成本控制 —— 默认唤醒包应控制在 1200 tokens 以内，且 95% 的普通唤醒请求不需要全库扫描。

NFR-4：本地响应性能 —— 在 100,000 条以内记忆记录规模下，普通唤醒请求应在 500ms 内返回，深检索请求应在 2s 内返回首批结果。

NFR-5：数据可解释性 —— 对任一条被保留、强化、遗忘或归档的记录，系统应保留可审查的原因字段或等价解释元数据。

NFR-6：可迁移性 —— 用户可通过文档化格式导出核心主体、关系、摘要与记忆数据，并在新实例中恢复，不依赖专有云服务。

NFR-7：稳定性 —— 核心记忆存储需支持异常恢复，单次异常退出后不得造成已确认写入记录的结构性损坏。

NFR-8：扩展性 —— MVP 的数据模型应允许后续增加月/季/年节律、更多主体分区与更丰富归档层，而无需重写全部基础结构。

NFR-9：隐私默认值 —— 系统默认本地保存，不主动上传、不默认同步、不默认将原始记忆内容发送至第三方服务。

NFR-10：可测试性 —— MVP 每一条核心链路均应具备自动化测试：初始化、写入、节律整理、唤醒包生成、召回、检索、归档导出。

**NFR 总计：10 项**

---

### 附加需求与约束

1. 代码基线：以 `mempalace-rs` 为主底座，扩展而非重写。
2. 接口优先：CLI 与 MCP 为第一阶段交付接口，Python API 为 Phase 2。
3. 存储约束：本地文件 + 结构化 SQLite + 可扩展检索索引共存。
4. 主体协议保留：UPSP 三个概念（共振度/valence/arousal）保留，其余舍弃。
5. 双轨记忆取消：改用"热度+主题"自动归档替代理性/感性分区。
6. 项目目录：Laputa，强调可移植性强兼容。
7. AGENTS.md：必须创建作为项目规范。
8. 归档导出格式：沿用 mempalace-rs 已有的结构，不重新定义专用打包格式。

### PRD 完整性评估

PRD 结构清晰，目标明确，功能需求 15 项、非功能需求 10 项均有完整描述。Open Questions 已全部在文档内解决。成功标准可衡量，非目标明确列出。整体完整性高，适合作为架构与实施的基础输入。

---

## Epic 覆盖率验证

### 覆盖矩阵

由于项目**尚未生成 Epics & Stories 文档**，以下矩阵基于架构文档中的「需求到组件映射」（Section 7.2）进行代偿分析：

| FR 编号 | PRD 需求摘要 | 架构支持位置 | 状态 |
|---------|------------|------------|------|
| FR-1 | 主体初始化 | `src/identity/`（ADR-007 P0） | ⚠️ 架构有映射，Epic 未建 |
| FR-2 | 日记写入 | `src/diary/`（ADR-006，P0） | ⚠️ 架构有映射，Epic 未建 |
| FR-3 | 记忆筛选 | `src/heat/`（ADR-003） | ⚠️ 架构有映射，Epic 未建 |
| FR-4 | 统一记忆记录 | `src/storage/memory.rs` | ⚠️ 架构有映射，Epic 未建 |
| FR-5 | 节律整理 | `src/rhythm/`（ADR-007 P2） | ⚠️ 架构有映射，Epic 未建 |
| FR-6 | 唤醒包生成 | `src/wakeup/`（ADR-004，HEAT-03） | ⚠️ 架构有映射，Epic 未建 |
| FR-7 | 时间流召回 | `src/searcher/timeline.rs`（P1 核心） | ⚠️ 架构有映射，Epic 未建 |
| FR-8 | 深度检索（RAG） | `src/searcher/semantic.rs`（D-004） | ⚠️ 架构有映射，Epic 未建 |
| FR-9 | 关系记忆 | `src/knowledge_graph/relation.rs` | ⚠️ 架构有映射，Epic 未建 |
| FR-10 | 情绪锚定 | `emotion_valence + emotion_arousal` 字段 | ⚠️ 架构有映射，Epic 未建 |
| FR-11 | 手动治理 | CLI/MCP `--important/--forget/--emotion anchor` | ⚠️ 架构有映射，Epic 未建 |
| FR-12 | 归档候选 | `src/archiver/marker.rs`（ADR-005） | ⚠️ 架构有映射，Epic 未建 |
| FR-13 | 归档导出 | `src/archiver/packer.rs`（Phase 2） | ⚠️ 架构有映射（Phase 2），Epic 未建 |
| FR-14 | 数据导出与迁移 | 沿用 mempalace-rs 结构 | ⚠️ 架构有映射，Epic 未建 |
| FR-15 | 工具化接口（CLI/MCP） | `src/cli/` + `src/mcp_server/`（ADR-009） | ⚠️ 架构有映射，Epic 未建 |

### 缺失覆盖

由于 Epics & Stories 文档完全缺失，**15/15 FR 均无 Epic 级别的实施分解**。

### 覆盖统计

- PRD 功能需求总计：**15 项**
- 架构已映射：**15 项（100%）**
- Epic 文档覆盖：**0 项（0%）**
- 覆盖结论：**需要创建 Epics & Stories 文档**

---

## UX 对齐评估

### UX 文档状态

**未找到** UX 设计文档。

### UX 需求性分析

天空之城 MVP 的主要交付接口为 **CLI 与 MCP**，PRD 明确将图形化桌面端列入非目标（Explicit Non-Goals 第 2 条）。架构文档中的用户感知方式（Section 3.6）仅涉及热度状态图标（🔥📌💤📦），属于 CLI 输出约定，而非 UI 设计。

**结论**：本项目 MVP 阶段为 CLI/MCP 工具型产品，不存在传统意义上的 UI 界面需求，**UX 文档缺失不构成阻塞性问题**。

### 警告

⚠️ **警告（低优先级）**：架构文档 Section 3.6 定义了热度状态的用户可见表示（图标 + 状态描述），但未形成正式 UX 文档。若后续阶段引入桌面端或 Web 界面，需补充 UX 规范。

---

## Epic 质量审查

### 审查结论

由于 Epics & Stories 文档**完全缺失**，无法执行 Epic 级别质量评审。以下为基于 PRD + 架构内容的**预判性问题清单**，供创建 Epics 时参考：

#### 关键风险（创建 Epics 时必须注意）

1. **FR-3「记忆筛选」逻辑边界不清晰**：PRD 中「存储、丢弃、合并或加热」四种保留判断的触发条件未被量化，架构文档中未见 MemoryGate 组件的详细决策规则。在创建 Epic/Story 时需明确筛选的具体算法或规则集，否则实施者无法独立完成。

2. **FR-5「节律整理」触发机制未定义**：PRD 说「在节律点触发」，架构文档 `src/rhythm/scheduler.rs` 有调度器占位，但调度方式未决策（定时器？CLI 手动触发？MCP 调用？）。这是创建对应 Story 的阻塞条件。

3. **FR-9「关系记忆」MVP 边界模糊**：PRD 说「至少支持关键关系字段与近期关系摘要」，但「关键关系字段」的具体定义未在 PRD 或架构中列出。架构中仅有 `relation.rs` 文件占位，无字段规范。

#### 主要问题

4. **FR-13「归档导出」Phase 归属需在 Epic 中明确标注**：PRD 将其列入 MVP Scope，但架构文档将 `packer.rs`（打包器）标注为 Phase 2。PRD 与架构存在轻微出入，需在 Epic 中明确 Phase 1 仅标记、Phase 2 才导出。

5. **FR-14「数据导出与迁移」缺乏具体格式规范**：PRD 说沿用 mempalace-rs 结构，但 mempalace-rs 的导出格式未在任何文档中明确说明。需在对应 Story 中引用或附上 mempalace-rs 导出格式参考。

6. **`identity/` 模块内容不足**：架构仅定义了 `initializer.rs` 和 `state.rs` 两个文件，但 FR-1 要求初始化「身份、状态、关系与记忆的最小数据结构」，关系字段如何融入 identity 模块尚不明确。

#### 次要问题

7. **NFR-4 性能目标缺乏测试基准**：500ms 唤醒 / 2s 深检索的指标已在 PRD/架构中出现，但架构文档中 `benches/bench_heat.rs` 仅为热度计算基准，缺少针对 wakeup 和 deep search 的性能基准测试规划。

8. **串行测试策略仅部分覆盖**：架构定义了 `#[serial]` 要求用于归档/热度测试，但未说明 E2E 集成测试（`test_mvp_flow.rs`）是否也需要串行化，创建 Story 时需明确。

---

## 汇总与建议

### 整体就绪状态

**NEEDS WORK（需要补充后再实施）**

PRD 与架构文档质量高、对齐良好，架构已通过内部自验证（Section 7 覆盖率 9/9 = 100%），具备坚实实施基础。但**缺少 Epics & Stories 文档**是当前最关键的阻塞项——没有 Epic/Story 分解，实施者（尤其是 AI Agent）无法以受控方式逐步交付。

### 必须立即解决的关键问题

| 优先级 | 问题 | 阻塞性 |
|--------|------|--------|
| P0 | 缺少 Epics & Stories 文档 | 是，直接阻塞实施 |
| P0 | FR-5 节律整理调度机制未决策 | 是，阻塞对应 Story 编写 |
| P1 | FR-3 记忆筛选决策规则未量化 | 部分阻塞 |
| P1 | FR-9 关系字段定义缺失 | 部分阻塞 |
| P1 | FR-13 归档 Phase 归属 PRD/架构出入 | 需澄清 |

### 推荐下一步行动

1. **立即创建 Epics & Stories 文档**：使用 `/bmad-create-epics-and-stories` 技能，基于本报告的 FR 列表和架构优先级（P0 主体初始化、P0 日记输入、P1 检索层、P1 唤醒上下文、P2 节律整理、P3 生命周期治理）分解为可执行 Story。

2. **决策 FR-5 调度机制**：在 Epic 创建前先明确节律整理的触发方式（建议 Phase 1 采用 CLI 手动触发，Phase 2 引入定时调度），并更新架构文档 `src/rhythm/scheduler.rs` 说明。

3. **补充 FR-9 关系字段定义**：在 `Laputa/DECISIONS.md` 中补充关系节点（Resonance 节点）的最小字段规范，或在架构文档中扩充 `knowledge_graph/relation.rs` 的字段定义。

4. **统一 FR-13 Phase 归属**：更新 PRD 第 6 节「归档候选」或架构第 6 节映射表，明确 Phase 1 交付「归档候选标记」，Phase 2 交付「归档打包与导出」，消除文档出入。

### 最终说明

本评估共发现 **8 个问题**，分布于 **3 个类别**（Epic 缺失 1 项、关键设计缺口 3 项、次要问题 4 项）。PRD 与架构两份文档本身质量优秀，核心阻塞仅为 Epics & Stories 尚未创建。建议在解决上述第 2~4 条澄清项后，立即推进 Epic 创建，即可进入实施阶段。

---

_评估完成时间：2026-04-13 | 评估者：实施就绪性检查工作流_
